extern func print_int(int) void;

package hello {
    var x,y int;
    func main() int {
        if (x + 1 > y && y <= x && x + 10 << 1 > y && !false && false || true) {
            print_int(5);
        }
        else {
            print_int(1);
        }
    }
}
