extern func print_int(int) void;
extern func print_string(string) void;

package EvansCompilerShredder {
    
    func foo() bool { 
        return(foo());
    }
    func bar() bool { }

    func main() int {
        if(bar() || foo())
        {
            print_string("A foo walks into a bar");
        }
        else
        {
            print_string("FUBAR");
        }
    }
}