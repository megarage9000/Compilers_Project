extern func print_int(int) void;
extern func print_string(string) void;

package booltoint {
    func getBool(anInt int) int {
        anInt = anInt - 1;
        return (anInt + 2 * 5);
    }

    func main() int {
        var convert bool;
        print_int(getBool(convert));
    }
}