extern func print_int(int) void;

package foo {
    func main() int {
        var x, y int;
        x = 1 >> 3 << 3 + 1;
        y = x + 1;
        if(x < 1){
            print_int(x / y);
        }
        print_int(y / x);
    }
}
