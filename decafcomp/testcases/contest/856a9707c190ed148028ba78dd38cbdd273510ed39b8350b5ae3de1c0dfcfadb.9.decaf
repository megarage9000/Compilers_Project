extern func print_int(int) void;
extern func print_string(string) void;

package foo {

	var foo [10] bool;

    func skct() bool {
        print_string("foo");
        return(false);
    }
    func main() int {
        var test bool;
        var i int;
        
        print_string("Start \n");
        for (i = 0; i < 10; i = i + 3) {
			foo[i] = true;
		}

		for (i = 0; i < 10; i = i + 1) {
			test = foo[i] || skct();
		}
		
		print_string("End \n");
		
    }
}
