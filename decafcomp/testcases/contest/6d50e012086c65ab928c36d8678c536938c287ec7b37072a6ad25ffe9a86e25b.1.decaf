package NoExternFuncs {
    var arr [10]int;

    func main() int {
        var i int;

        for (i = 0; arr[i] < 5; i = i + 1, arr[i] = i) {
            // just a boring loop, can't print anything
            continue;
        }
    }
}