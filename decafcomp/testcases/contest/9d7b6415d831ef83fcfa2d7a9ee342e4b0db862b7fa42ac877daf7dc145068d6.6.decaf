extern func print_string(string) void;
extern func print_int(int) void;

package Test {
    var x, y, z int;
    var a, b, c bool;

    func main() int {
        x = 1;
        y = 2;
        z = 3;

        print_int(x + (x * (y * y)) - (3 + (z - x)));
        print_int(a || !b || !c);
    }
}