extern func print_int(int) void;
extern func print_string(string) void;

package foo {

    func main() int {
      var x, y int;
      for(x = 0, y = 4; x != 2; x = x + 1, y = y - 1){
        print_int(x); print_int(y);
      }

    }
}